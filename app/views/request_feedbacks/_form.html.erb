<%= form_with(model: request_feedback, local: true) do |form| %>
  <% if request_feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_feedback.errors.count, "error") %> prohibited this request_feedback from being saved:</h2>

      <ul>
      <% request_feedback.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
